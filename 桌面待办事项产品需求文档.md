# 桌面待办事项产品需求文档

> 产品工作名：桌面待办  
> 文档版本：V1.0 Draft  
> 文档状态：需求确认稿  
> 最后更新：2026-08-20  
> 适用平台：Windows 10 22H2、Windows 11（64 位）

## 1. 文档信息

### 1.1 版本记录

| 版本 | 日期 | 状态 | 说明 |
|---|---|---|---|
| V0.1 | 2026-08-20 | 初稿 | 定义桌面小组件、任务、类型、颜色、截止时间和提醒能力 |
| V0.2 | 2026-08-20 | 修订 | 增加跨电脑导出、自动合并、UUID 去重、删除标记及未来同步预留 |
| V1.0 Draft | 2026-08-20 | 需求确认稿 | 补充加密导出、异常处理、数据模型和验收标准 |

### 1.2 优先级定义

| 优先级 | 含义 |
|---|---|
| P0 | 首版必须具备；缺失时不得发布 |
| P1 | 首版应具备；如受技术限制延期，必须明确记录并获得确认 |
| P2 | 后续版本候选；首版只预留扩展空间 |

### 1.3 术语

| 术语 | 定义 |
|---|---|
| 待办 | 尚未标记完成且未被删除的任务 |
| 已完成 | 已记录完成时间且未被删除的任务 |
| 桌面嵌入模式 | 小组件显示在桌面层，位于普通应用窗口之后 |
| 置顶模式 | 小组件位于普通应用窗口之上，但不覆盖锁屏、UAC 等系统安全界面 |
| 合并导入 | 按实体 UUID 和版本信息，将导入数据与本机数据自动合并 |
| 覆盖恢复 | 用导入文件中的业务数据整体替换本机业务数据 |
| 删除标记 | 对用户不可见的内部删除记录，用于将删除操作传播到其他设备 |
| 设备设置 | 只适用于当前电脑、不参与跨设备合并的窗口和系统集成设置 |

## 2. 产品概述

### 2.1 产品定位

“桌面待办”是一款面向个人用户的 Windows 本地待办事项小组件。产品以快速记录、持续可见、低打扰和本地隐私为核心，不要求注册账号，不依赖网络即可完成全部首版功能。

产品虽然首版不接入服务器，但任务、类型、颜色、导入导出和数据访问层必须为后期数据库同步预留稳定标识、版本信息和冲突处理能力。

### 2.2 目标用户

- 需要在工作电脑或家庭电脑上快速记录个人事项的 Windows 用户。
- 希望待办常驻桌面，但不希望遮挡日常应用窗口的用户。
- 需要通过文件在两台电脑之间手动转移、继续编辑并合并待办事项的用户。

### 2.3 产品目标

1. 用户可在桌面小组件内快速完成任务的添加、查看、编辑、完成、恢复和删除。
2. 默认状态不遮挡普通应用窗口，需要时可一键置顶。
3. 清晰展示任务的类型、颜色、截止时间、剩余时间和逾期状态。
4. 在应用运行或驻留托盘时可靠发送 Windows 通知。
5. 支持通过文件在不同电脑之间双向迁移并自动合并数据。
6. 所有首版核心能力在无网络环境下可用。

### 2.4 首版不包含

- 用户账号、联网数据库、云同步及多人协作。
- 手机端、Web 端及其他操作系统客户端。
- 重复任务、子任务、附件和富文本编辑。
- 任务优先级、标签、全文搜索、复杂筛选和手动拖拽排序。
- 日历、邮件、即时通信及第三方平台集成。
- 便携版、插件系统和自动在线更新。
- 自定义皮肤、复杂动画及多套自定义布局。

## 3. 信息架构与核心流程

### 3.1 主界面结构

主界面为单一紧凑面板，从上到下包含：

1. 标题栏：产品名称、置顶开关、设置入口。
2. 标签栏：“待办”和“已完成”两个标签，并显示实时数量。
3. 任务列表：按当前标签展示任务，内容超出时在面板内滚动。
4. 添加入口：固定在易于访问的位置，不随任务列表滚出可视区域。
5. 编辑区：添加或编辑任务时在当前面板内展开，不另开大型窗口。

### 3.2 核心流程

#### 添加任务

打开添加区 → 输入标题 → 可选填写备注 → 选择类型和颜色 → 可选设置截止时间及提醒 → 保存 → 任务进入待办列表并自动排序。

#### 完成与恢复

勾选待办 → 记录完成时间 → 自动移入已完成列表。  
在已完成列表选择恢复 → 清除完成时间 → 移回待办列表并重新排序。

#### 跨电脑继续使用

电脑 A 导出数据文件 → 将文件带到电脑 B → 电脑 B 选择合并导入 → 系统预检并自动合并 → 在电脑 B 继续编辑 → 再次导出并导回电脑 A → 电脑 A 按相同规则合并。

## 4. 功能需求

### 4.1 窗口与桌面小组件

| 编号 | 优先级 | 需求 |
|---|---|---|
| WIN-001 | P0 | 应用默认以桌面嵌入模式启动，显示在桌面壁纸之上、普通应用窗口之后。 |
| WIN-002 | P0 | 用户可一键切换置顶模式；再次关闭置顶后立即回到桌面嵌入模式。 |
| WIN-003 | P0 | 应用记住上次选择的桌面嵌入/置顶模式，并在下次启动时恢复。 |
| WIN-004 | P0 | 小组件采用无边框面板，支持拖动和缩放。默认尺寸为 360 × 520 DIP，最小尺寸为 320 × 360 DIP。 |
| WIN-005 | P0 | 应用保存窗口位置、尺寸和所在显示器；分辨率或显示器变化后必须将窗口修正到可见区域。 |
| WIN-006 | P0 | 支持多显示器和 Windows 100%～200% DPI 缩放，缩放变化后文字和控件不得裁切。 |
| WIN-007 | P0 | Windows Explorer 重启后，应用应自动恢复桌面层绑定，无需重启应用。 |
| WIN-008 | P0 | 点击关闭按钮时隐藏到系统托盘，不结束应用进程和提醒服务。 |
| WIN-009 | P0 | 托盘菜单至少提供“显示小组件”“快速添加”“切换置顶”“设置”“退出”。 |
| WIN-010 | P0 | 用户选择“退出”后结束进程并停止提醒；下次启动时对错过的提醒进行一次补查。 |
| WIN-011 | P1 | 设置中提供“随 Windows 启动”开关，默认关闭。 |
| WIN-012 | P0 | 应用只允许一个运行实例；重复启动时激活并显示已有实例。 |
| WIN-013 | P0 | 置顶模式不得覆盖锁屏、UAC、安全桌面等 Windows 系统安全界面。 |

补充说明：桌面嵌入模式保证不遮挡普通应用窗口，但小组件自身区域需要接收鼠标操作，因此可能覆盖该区域内的桌面图标；首版不做桌面图标碰撞检测或自动避让。

### 4.2 列表与任务生命周期

| 编号 | 优先级 | 需求 |
|---|---|---|
| TASK-001 | P0 | 主界面提供“待办”“已完成”两个标签，标签数量不包含内部删除标记。 |
| TASK-002 | P0 | 默认打开待办标签；切换标签后保持当前窗口位置和尺寸。 |
| TASK-003 | P0 | 用户可添加、编辑待办任务。已完成任务只读，恢复为待办后方可编辑。 |
| TASK-004 | P0 | 勾选待办后，任务立即标记完成、记录完成时间并移入已完成列表。 |
| TASK-005 | P0 | 已完成任务可恢复；恢复时清除完成时间，保留标题、备注、类型、颜色、截止时间及提醒设置。 |
| TASK-006 | P0 | 待办和已完成任务均可删除，删除前必须二次确认。 |
| TASK-007 | P0 | 完成或删除任务时取消该任务尚未触发的提醒。恢复已完成任务时不自动恢复已经取消的提醒。 |
| TASK-008 | P0 | 已完成列表按完成时间倒序排列；完成时间相同时按更新时间倒序排列。 |
| TASK-009 | P0 | 每次添加、编辑、完成、恢复、删除后自动保存，不依赖额外“保存全部”操作。 |
| TASK-010 | P0 | 列表为空时显示对应空状态和添加引导，不显示空白面板。 |

### 4.3 任务字段与校验

| 字段 | 必填 | 规则 |
|---|---|---|
| 标题 | 是 | 去除首尾空格后不能为空；最多 200 个字符 |
| 备注 | 否 | 纯文本；最多 2000 个字符；列表默认折叠显示 |
| 类型 | 否 | 未选择时使用系统类型“未分类” |
| 颜色 | 否 | 默认跟随类型颜色；允许选择调色板中的颜色进行单任务覆盖 |
| 期望完成时间 | 否 | 精确到分钟；允许保存过去的时间，但需要显示“已逾期”提示 |
| 提醒 | 否 | 每条任务最多一个有效提醒；提醒时间必须晚于保存时刻 |

| 编号 | 优先级 | 需求 |
|---|---|---|
| EDIT-001 | P0 | 点击添加后在当前小组件内展开编辑区，类型和颜色选择器必须直接可见或一步可达。 |
| EDIT-002 | P0 | 同一时刻最多打开一个任务编辑区；切换前如有未保存内容，提示保存、放弃或取消切换。 |
| EDIT-003 | P0 | 保存成功后关闭编辑区并定位到新建或更新后的任务。 |
| EDIT-004 | P0 | 校验失败时保留用户输入，并在对应字段附近显示明确原因。 |
| EDIT-005 | P1 | 支持基础键盘操作：新建、保存、取消、切换标签、完成当前任务。 |

### 4.4 待办排序

待办列表不提供手动拖拽，按下列规则自动排序：

1. 已逾期任务排在最前，截止时间越早越靠前。
2. 未逾期且有截止时间的任务随后排列，距离截止时间越近越靠前。
3. 无截止时间的任务排在最后，按创建时间倒序排列。
4. 前述排序键完全相同时，按更新时间倒序，再按 UUID 字典序排列，保证结果稳定。

### 4.5 类型管理

| 编号 | 优先级 | 需求 |
|---|---|---|
| TYPE-001 | P0 | 设置页提供类型列表，支持添加、重命名、修改默认颜色、调整显示顺序和删除。 |
| TYPE-002 | P0 | 类型名称去除首尾空格后不能为空，最多 20 个字符，忽略大小写后不得重复。 |
| TYPE-003 | P0 | 系统内置“未分类”类型，作为缺失引用的兜底，不允许删除或重命名。 |
| TYPE-004 | P0 | 每个类型必须指定一个默认颜色。 |
| TYPE-005 | P0 | 修改类型默认颜色后，使用“跟随类型颜色”的任务同步变化；单独改色的任务不变化。 |
| TYPE-006 | P0 | 删除被任务引用的类型前显示受影响任务数量。确认删除后，任务改为“未分类”，并将删除前显示的颜色保存为任务覆盖色。 |

### 4.6 颜色管理

| 编号 | 优先级 | 需求 |
|---|---|---|
| COLOR-001 | P0 | 设置页提供颜色列表，支持添加、修改名称和颜色值、调整顺序及删除。 |
| COLOR-002 | P0 | 颜色名称不能为空、最多 20 个字符；颜色值使用 6 位十六进制 RGB。 |
| COLOR-003 | P0 | 内置不可删除的中性灰兜底色；另预置蓝、绿、黄、红、紫五种可编辑基础色。 |
| COLOR-004 | P0 | 删除被类型或任务引用的颜色时，用户必须先选择替代颜色；确认后一次性替换全部引用。 |
| COLOR-005 | P0 | 颜色只作为辅助标识；完成、逾期、提醒等状态必须同时使用文字、图标或形状表达。 |

建议的初始调色板：

| 名称 | 颜色值 | 说明 |
|---|---|---|
| 中性灰 | `#6B7280` | 系统兜底，不可删除 |
| 蓝色 | `#3B82F6` | 可编辑 |
| 绿色 | `#22C55E` | 可编辑 |
| 黄色 | `#F59E0B` | 可编辑 |
| 红色 | `#EF4444` | 可编辑 |
| 紫色 | `#8B5CF6` | 可编辑 |

### 4.7 截止时间与剩余时间

| 编号 | 优先级 | 需求 |
|---|---|---|
| TIME-001 | P0 | 截止时间按 Windows 当前本地时区输入和展示，内部统一转换为 UTC 保存。 |
| TIME-002 | P0 | 有截止时间的待办显示动态剩余时间，例如“剩余 2 天 3 小时”“剩余 45 分钟”“即将到期”“已逾期 1 小时”。 |
| TIME-003 | P0 | 剩余时间至少每分钟刷新一次；不足 1 分钟时显示“即将到期”。 |
| TIME-004 | P0 | 逾期状态使用独立文字或图标，不得只依赖用户选择的红色。 |
| TIME-005 | P0 | 已完成列表不再显示动态倒计时，但保留原截止时间和实际完成时间。 |
| TIME-006 | P0 | Windows 时区发生变化后，绝对时间保持不变，界面按新本地时区重新展示。 |

### 4.8 提醒与 Windows 通知

每条任务最多有一个有效提醒，默认不自动开启。设置截止时间后，用户可选择：

- 到期时提醒。
- 提前 10 分钟。
- 提前 1 小时。
- 提前 1 天。
- 自定义提醒时间。

没有截止时间时，只允许设置未来的自定义提醒时间。

| 编号 | 优先级 | 需求 |
|---|---|---|
| REM-001 | P0 | 使用 Windows 原生通知展示任务标题、类型、截止时间和剩余/逾期状态。 |
| REM-002 | P0 | 通知支持“打开任务”“标记完成”“稍后提醒”。 |
| REM-003 | P0 | 稍后提醒提供 10 分钟、1 小时、明天同一时间三个选项，不修改任务截止时间。 |
| REM-004 | P0 | 相对提醒在修改截止时间后自动重新计算；自定义提醒保持原绝对时间。 |
| REM-005 | P0 | 修改、完成或删除任务时，必须取消旧提醒并按任务最新状态决定是否重新注册。 |
| REM-006 | P0 | 小组件隐藏到托盘时提醒正常工作。用户主动退出后停止提醒。 |
| REM-007 | P0 | 设备休眠、应用异常退出或主动退出导致提醒错过时，下次运行后每个任务最多补发一次。 |
| REM-008 | P0 | 点击通知打开任务时临时将窗口带到前台并定位该任务；交互结束后恢复用户配置的窗口层级。 |
| REM-009 | P0 | Windows 通知权限被关闭时，在设置页显示状态说明和系统通知设置入口。 |
| REM-010 | P1 | 应用运行期间，通知触发时间相对计划时间的偏差不超过 60 秒。 |

## 5. 本地数据、导出与导入

### 5.1 本地存储

| 编号 | 优先级 | 需求 |
|---|---|---|
| DATA-001 | P0 | 所有数据保存在当前 Windows 用户的数据目录，不要求管理员权限。 |
| DATA-002 | P0 | 首版运行期间不得上传任务、类型、颜色、使用行为或设备标识。 |
| DATA-003 | P0 | 数据变更使用事务或原子写入；异常中断不得产生部分更新。 |
| DATA-004 | P0 | 安装升级不得破坏既有数据；卸载默认保留数据，仅在用户明确勾选后删除。 |
| DATA-005 | P0 | 所有同步实体使用永久稳定的 UUID。创建后不得因编辑、完成、恢复、导出或导入而改变。 |
| DATA-006 | P0 | 本机生成永久 `deviceId`，仅用于版本来源和确定性合并，不用于联网追踪。 |

### 5.2 导出文件

首版导出完整业务数据快照，包括可见任务、内部删除标记、类型和颜色；不导出窗口位置、窗口尺寸、置顶状态、开机启动等设备设置。

| 编号 | 优先级 | 需求 |
|---|---|---|
| EXPORT-001 | P0 | 导出时提供“密码加密文件”和“明文 JSON 文件”两种方式，默认突出并推荐加密方式。 |
| EXPORT-002 | P0 | 明文文件扩展名使用 `.dtodo.json`，内容为 UTF-8 JSON。 |
| EXPORT-003 | P0 | 加密文件扩展名使用 `.dtodo`，不得在文件中保存密码或可直接恢复密码的信息。 |
| EXPORT-004 | P0 | 加密文件使用带完整性校验的认证加密；实现基线为 Argon2id 派生密钥和 AES-256-GCM 加密。盐、随机数和算法参数可保存在文件头。 |
| EXPORT-005 | P0 | 加密密码最少 10 个字符，支持中文及其他 Unicode 字符；导出前要求再次输入确认。 |
| EXPORT-006 | P0 | 导出包包含 `schemaVersion`、`exportId`、`exportedAt`、`sourceDeviceId` 和完整业务实体。 |
| EXPORT-007 | P0 | 导出采用临时文件加原子替换；失败时不得留下看似成功但内容不完整的目标文件。 |
| EXPORT-008 | P0 | 明文导出前明确提示文件内容可直接读取；加密文件密码丢失后无法恢复。 |

### 5.3 导入模式

导入提供两种模式：

1. **合并导入（默认）**：根据 UUID 和版本信息与本机数据自动合并。
2. **覆盖恢复（高级）**：以文件内业务数据整体替换本机业务数据；必须二次确认。

两种模式均必须在修改数据前创建本机快照。快照仅保存在本机应用数据目录，并记录创建时间和来源文件名。

### 5.4 合并规则

#### 实体身份

- 相同 UUID 视为同一实体。
- 不使用标题、备注、类型名称等内容进行模糊去重。
- UUID 不同，即使标题和内容完全相同，也保留为两个独立任务。

#### 版本比较

对相同 UUID 的实体，按以下顺序比较：

1. `updatedAt` 较新的版本获胜。
2. `updatedAt` 相同时，`revision` 较大的版本获胜。
3. 两者仍相同时，如内容哈希一致，则视为完全相同并跳过。
4. 两者仍相同但内容不同，按 `updatedByDeviceId` 字典序确定唯一胜者，并在导入结果中记录“确定性冲突”。

完成、恢复、类型变化、颜色变化、提醒变化和删除均属于一次实体修改，必须更新 `updatedAt`、增加 `revision` 并写入当前 `updatedByDeviceId`。

#### 删除传播

- 用户删除任务、类型或颜色时，界面立即隐藏对应实体，内部保留 `deletedAt` 删除标记。
- 删除标记参与导出和版本比较；较新的删除版本可以删除另一台电脑上的旧记录。
- 如果另一台电脑在删除之后产生了时间更晚的有效编辑，则更新版本获胜，实体可以重新出现。
- 首版不自动物理清除删除标记，以免旧导出文件使已删除数据复活。
- 删除标记不提供用户可见的回收站能力。

#### 引用处理

- 导入时先处理颜色和类型，再处理任务。
- 任务引用缺失或已删除的类型时，自动改为“未分类”。
- 任务或类型引用缺失或已删除的颜色时，自动改为中性灰。
- 引用修复必须写入导入摘要，但不应导致整个文件导入失败。

### 5.5 导入执行流程

| 步骤 | 行为 |
|---|---|
| 1. 读取 | 识别文件类型；加密文件要求输入密码 |
| 2. 校验 | 校验文件完整性、格式、`schemaVersion`、字段类型和实体 UUID |
| 3. 预览 | 显示来源设备、导出时间、任务/类型/颜色数量和预计操作 |
| 4. 快照 | 在本机创建导入前快照；快照失败则禁止继续 |
| 5. 执行 | 在单个事务中合并或覆盖业务数据 |
| 6. 修复 | 修复无效类型、颜色引用，重新注册有效提醒 |
| 7. 提交 | 全部成功后一次性提交；任一步失败则回滚 |
| 8. 汇总 | 展示新增、更新、保留本机、删除、跳过、引用修复和异常数量 |

### 5.6 导入异常

| 场景 | 预期行为 |
|---|---|
| 密码错误 | 显示密码错误，不读取业务内容，不修改本机数据 |
| 文件损坏或认证失败 | 显示文件完整性校验失败，不修改本机数据 |
| JSON 格式错误 | 指出格式无效，不修改本机数据 |
| 缺少必填字段 | 将文件判定为无效，不进行部分导入 |
| UUID 格式错误 | 将对应文件判定为无效，不进行部分导入 |
| 版本过新 | 提示当前应用不支持该文件版本，不修改数据 |
| 旧版本可迁移 | 在内存或临时副本中迁移，通过校验后再导入 |
| 磁盘空间不足 | 中止操作并保留原数据及已存在快照 |
| 导入中应用退出 | 事务回滚；下次启动时提示上次导入未完成 |

### 5.7 时间与时钟限制

- `createdAt`、`updatedAt`、`completedAt`、`deletedAt`、`dueAt`、`reminderAt` 统一以 UTC 保存，精确到毫秒。
- 界面按当前 Windows 本地时区展示。
- 首版“最后编辑版本获胜”依赖两台电脑的系统时间基本准确。设置页应提示用户启用 Windows 自动校时。
- `revision` 和 `updatedByDeviceId` 用于同时间戳下的确定性处理，不替代中央服务器版本。
- 后续接入联网数据库后，可增加服务端版本或同步游标，不改变现有实体 UUID。

## 6. 需求级数据模型

### 6.1 Task

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | UUID | 任务永久唯一标识 |
| `title` | string | 任务标题，1～200 字符 |
| `note` | string/null | 纯文本备注，最多 2000 字符 |
| `status` | `TODO` / `DONE` | 当前状态 |
| `typeId` | UUID | 类型标识；无有效引用时指向“未分类” |
| `colorMode` | `INHERIT` / `OVERRIDE` | 跟随类型或单任务改色 |
| `colorId` | UUID/null | 覆盖颜色；跟随类型时为空 |
| `dueAt` | UTC datetime/null | 期望完成时间 |
| `reminderMode` | enum | 无、到期、提前 10 分钟、提前 1 小时、提前 1 天、自定义 |
| `reminderAt` | UTC datetime/null | 计算后的实际提醒时间 |
| `reminderFiredAt` | UTC datetime/null | 最近一次已触发时间，用于避免重复补发 |
| `createdAt` | UTC datetime | 创建时间 |
| `updatedAt` | UTC datetime | 最后业务修改时间 |
| `completedAt` | UTC datetime/null | 完成时间 |
| `deletedAt` | UTC datetime/null | 删除标记时间 |
| `revision` | uint64 | 本实体修改版本，每次业务修改递增 |
| `updatedByDeviceId` | UUID | 最后修改设备 |

### 6.2 TaskType

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | UUID | 类型永久唯一标识 |
| `name` | string | 类型名称 |
| `defaultColorId` | UUID | 默认颜色 |
| `sortOrder` | integer | 选择器显示顺序 |
| `isSystem` | boolean | 是否为不可删除的系统类型 |
| `createdAt` / `updatedAt` / `deletedAt` | UTC datetime | 生命周期时间 |
| `revision` | uint64 | 修改版本 |
| `updatedByDeviceId` | UUID | 最后修改设备 |

### 6.3 ColorDefinition

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | UUID | 颜色永久唯一标识 |
| `name` | string | 颜色名称 |
| `hexValue` | string | 6 位 RGB 值 |
| `sortOrder` | integer | 选择器显示顺序 |
| `isSystem` | boolean | 是否为不可删除的兜底颜色 |
| `createdAt` / `updatedAt` / `deletedAt` | UTC datetime | 生命周期时间 |
| `revision` | uint64 | 修改版本 |
| `updatedByDeviceId` | UUID | 最后修改设备 |

### 6.4 DeviceSettings

以下字段只保存在本机，不参与导出合并：

- `deviceId`
- 窗口层级模式
- 窗口位置、尺寸和显示器标识
- 是否随 Windows 启动
- 最近打开的标签
- 通知权限提示状态

### 6.5 导出包结构

明文 JSON 的顶层至少包含：

```json
{
  "schemaVersion": 1,
  "exportId": "UUID",
  "exportedAt": "UTC ISO-8601",
  "sourceDeviceId": "UUID",
  "tasks": [],
  "types": [],
  "colors": []
}
```

加密文件解密后必须得到相同的逻辑结构。后续版本只能通过提高 `schemaVersion` 演进格式，不得复用同一版本号表达不兼容结构。

## 7. 状态与业务规则

### 7.1 任务状态转换

```text
新建 → 待办
待办 → 编辑 → 待办
待办 → 完成 → 已完成
已完成 → 恢复 → 待办
待办/已完成 → 删除 → 删除标记
删除标记 → 更晚的跨设备编辑版本 → 待办或已完成
```

### 7.2 更新时间规则

以下操作必须更新实体的 `updatedAt`、`revision` 和 `updatedByDeviceId`：

- 修改任务任何业务字段。
- 完成或恢复任务。
- 删除实体。
- 修改类型名称、默认颜色或顺序。
- 修改颜色名称、颜色值或顺序。

仅查看任务、切换标签、移动窗口、切换置顶和修改本机设置不得改变任务版本。

## 8. 设置

设置页至少包含：

- 类型管理。
- 颜色管理。
- 随 Windows 启动开关。
- Windows 通知权限状态与系统设置入口。
- 数据导出。
- 数据导入：默认合并导入、次级入口覆盖恢复。
- 本机数据目录和应用版本信息。
- 隐私说明：首版不联网、不上传数据。

## 9. 非功能要求

### 9.1 性能

| 编号 | 优先级 | 要求 |
|---|---|---|
| NFR-001 | P1 | 常规电脑冷启动目标不超过 2 秒。 |
| NFR-002 | P1 | 后台空闲时不得持续高频轮询，CPU 使用应接近 0。 |
| NFR-003 | P1 | 空闲内存目标不高于 150 MB。 |
| NFR-004 | P0 | 1 万条任务数据下，打开列表、切换标签和保存单条任务不得出现明显阻塞。 |

### 9.2 可靠性

- 已确认的本地操作在正常保存成功后不得因应用异常退出而丢失。
- 导入、类型删除、颜色替换等多记录操作必须使用事务。
- 应用启动时检查上次未完成事务和提醒状态，并进行安全恢复。
- 本地数据库损坏时不得静默覆盖；应停止写入并提示用户从快照或导出文件恢复。

### 9.3 安全与隐私

- 首版核心功能完全离线，不包含遥测和后台网络请求。
- 密码只在导出/导入操作期间驻留内存，使用后尽快清理，不写入日志。
- 日志不得记录任务标题、备注、密码或完整导出内容。
- 正式安装包应使用有效发布者证书签名。

### 9.4 可用性与无障碍

- 默认跟随 Windows 深色/浅色模式。
- 支持 100%～200% DPI，文本放大后关键操作仍可访问。
- 控件提供可读名称和键盘焦点状态，满足 Windows 屏幕阅读器的基础识别。
- 颜色不得作为唯一信息载体。
- 所有不可逆操作必须有清晰确认文案。

### 9.5 兼容性

- 支持 Windows 10 22H2 和当前受支持的 Windows 11 版本。
- 首版仅提供 64 位每用户安装包，不要求管理员权限。
- 升级安装保留本地数据库和设备设置。
- 卸载默认保留用户数据；用户主动选择删除数据时再次确认。

## 10. 异常与边界场景

| 场景 | 预期处理 |
|---|---|
| 应用在其他窗口全屏时处于桌面嵌入模式 | 不覆盖全屏应用 |
| 置顶模式遇到 UAC 或锁屏 | 服从 Windows 安全界面层级 |
| 外接显示器被拔除 | 将小组件移回主显示器可见区域 |
| Explorer 重启 | 自动重新绑定桌面层 |
| 通知权限被关闭 | 设置页提示并提供系统入口 |
| 截止时间已过去 | 允许保存，立即显示逾期 |
| 相对提醒重新计算后落入过去 | 要求用户重新选择未来提醒或关闭提醒 |
| 类型或颜色引用丢失 | 使用系统兜底类型/颜色并记录修复 |
| 同标题不同 UUID | 保留两条，不自动合并 |
| 相同 UUID 两台电脑都修改 | 按版本比较规则确定唯一结果 |
| 系统时间明显错误 | 提示检查 Windows 自动校时；首版不尝试推断真实先后 |
| 导入文件部分记录无效 | 整体拒绝导入，不做部分写入 |
| 导入过程中磁盘写入失败 | 回滚事务并保留导入前数据 |

## 11. 验收标准

### 11.1 核心任务

| 用例 | 操作 | 预期结果 |
|---|---|---|
| AC-TASK-01 | 添加仅含标题的任务 | 任务保存到待办列表，使用未分类和默认颜色 |
| AC-TASK-02 | 添加包含全部字段的任务 | 标题、备注、类型、覆盖色、截止时间和提醒均正确保存 |
| AC-TASK-03 | 编辑待办 | 列表立即更新，UUID 不变，版本信息递增 |
| AC-TASK-04 | 完成待办 | 任务移入已完成、记录完成时间、取消提醒 |
| AC-TASK-05 | 恢复已完成 | 任务返回待办并按截止时间重新排序 |
| AC-TASK-06 | 删除任务后取消确认 | 任务保持不变 |
| AC-TASK-07 | 确认删除任务 | 任务从界面消失，内部生成删除标记 |
| AC-TASK-08 | 创建同标题任务 | 生成不同 UUID，两条任务均保留 |

### 11.2 类型与颜色

| 用例 | 操作 | 预期结果 |
|---|---|---|
| AC-META-01 | 修改类型默认色 | 跟随类型的任务同步更新，覆盖色任务不变 |
| AC-META-02 | 删除使用中的类型 | 显示影响数量；确认后任务归入未分类并保持原显示色 |
| AC-META-03 | 删除使用中的颜色 | 必须选择替代色；类型和任务引用一次性更新 |
| AC-META-04 | 尝试删除系统兜底项 | 操作被拒绝并说明原因 |

### 11.3 时间与提醒

| 用例 | 操作 | 预期结果 |
|---|---|---|
| AC-TIME-01 | 创建未来截止任务 | 正确显示剩余时间并每分钟刷新 |
| AC-TIME-02 | 截止时间跨过当前时间 | 状态从剩余自动切换为逾期 |
| AC-TIME-03 | 修改带相对提醒的截止时间 | 提醒时间自动重算并重新注册 |
| AC-REM-01 | 提醒到点且应用在托盘 | 收到 Windows 通知 |
| AC-REM-02 | 在通知中标记完成 | 任务完成且不再重复提醒 |
| AC-REM-03 | 选择稍后 1 小时 | 截止时间不变，提醒推迟 1 小时 |
| AC-REM-04 | 设备休眠跨过提醒时间 | 恢复后补发一次，不连续重复 |
| AC-REM-05 | 用户主动退出跨过提醒时间 | 下次启动补发一次 |

### 11.4 窗口与系统集成

| 用例 | 操作 | 预期结果 |
|---|---|---|
| AC-WIN-01 | 普通应用窗口覆盖小组件区域 | 桌面嵌入模式下小组件位于其后 |
| AC-WIN-02 | 开启置顶 | 小组件显示在普通窗口之上 |
| AC-WIN-03 | 重启 Explorer | 小组件自动恢复桌面嵌入行为 |
| AC-WIN-04 | 拔除小组件所在显示器 | 小组件回到主显示器可见区域 |
| AC-WIN-05 | 重复启动应用 | 不创建第二实例，显示已有窗口 |
| AC-WIN-06 | 点击关闭按钮 | 窗口隐藏到托盘，提醒继续运行 |

### 11.5 导出、导入与合并

| 用例 | 操作 | 预期结果 |
|---|---|---|
| AC-SYNC-01 | 明文导出 | 生成有效 UTF-8 `.dtodo.json` 文件并提示明文风险 |
| AC-SYNC-02 | 密码加密导出 | 生成不可直接读取业务内容的 `.dtodo` 文件 |
| AC-SYNC-03 | 用正确密码导入 | 通过校验并进入预览 |
| AC-SYNC-04 | 用错误密码导入 | 明确报错，本机数据完全不变 |
| AC-SYNC-05 | 导入损坏文件 | 完整性校验失败，本机数据完全不变 |
| AC-SYNC-06 | 导入相同文件两次 | 第二次全部跳过，不重复生成任务 |
| AC-SYNC-07 | 相同 UUID 导入版本较新 | 导入版本覆盖本机版本 |
| AC-SYNC-08 | 相同 UUID 本机版本较新 | 保留本机版本并计入汇总 |
| AC-SYNC-09 | 相同标题但 UUID 不同 | 两条任务同时保留 |
| AC-SYNC-10 | 导入较新的删除标记 | 本机旧任务被删除且不再显示 |
| AC-SYNC-11 | 导入晚于删除的有效编辑 | 较新的编辑版本获胜，任务重新出现 |
| AC-SYNC-12 | 公司导出→家庭编辑→公司导回 | 新任务被新增、旧任务按最后编辑合并、未改任务不重复 |
| AC-SYNC-13 | 导入中模拟写入失败 | 整体回滚，可从导入前快照恢复 |
| AC-SYNC-14 | 导入高版本格式 | 拒绝导入并提示升级应用 |
| AC-SYNC-15 | 执行覆盖恢复 | 二次确认后替换业务数据，设备设置保持不变 |

### 11.6 发布门槛

满足以下条件方可发布首版：

1. 所有 P0 需求完成并通过验收。
2. Windows 10 22H2 与 Windows 11 分别完成完整回归。
3. 桌面嵌入、置顶切换、Explorer 重启恢复、多显示器迁移通过稳定性测试。
4. 双向文件迁移、删除传播、加密导出和事务回滚通过测试。
5. 安装、升级、卸载保留数据流程通过测试。
6. 不存在会导致任务静默丢失、重复或错误覆盖的已知严重问题。

## 12. 技术预研要求

正式开发前需完成一个 Windows 桌面层技术验证原型，至少验证：

- 在 Windows 10/11 上稳定嵌入桌面层。
- 桌面嵌入与置顶模式可即时切换。
- Explorer 重启后可重新绑定。
- 多显示器、DPI 切换和显示器拔插后窗口可恢复。
- 托盘驻留、单实例和 Windows 通知行为符合需求。

预研只验证系统能力和稳定性，不改变本需求文档定义的产品行为。若操作系统限制导致某项行为无法可靠实现，应记录复现条件、影响范围和替代方案，重新进行需求评审。

## 13. 未来扩展预留

首版不实现联网同步，但实现时应保证：

- UI、业务服务和本地数据访问相互分离，导入合并逻辑可被未来同步服务复用。
- UUID、版本、删除标记和设备来源字段不依赖具体本地数据库实现。
- 后续可增加服务器版本、同步游标、账号归属和传输加密，而不更换现有实体 ID。
- 未来云同步的冲突策略可由“本地 UTC 最后编辑优先”升级为“服务端确认版本优先”。
- 备份文件继续保持版本化，确保旧文件可迁移或得到明确的不兼容提示。

---

本文件中的“桌面待办”为产品工作名，正式命名可在不改变需求编号、数据语义和验收标准的前提下统一替换。
