# AI-00 契约与安全基线验收记录

## 阶段状态

- 阶段：AI-0（契约与安全基线）
- 当前状态：待用户确认
- 范围：只建立 Rust/TypeScript 契约、风险分级和稳定错误码。

## 已完成

- Rust 契约位于 `app/windows-client/src-tauri/src/mcp.rs`。
- 前端类型镜像位于 `app/windows-client/src/mcp/contracts.ts`。
- 协议版本固定为 `mylist.mcp.v1`。
- 工具按只读、写入、高风险分级；删除、导入导出等高风险工具要求确认。
- 工具名称与需求文档 4.2–4.5 对齐，使用 `mylist_` 前缀；预检工具不直接修改数据，确认工具标记为高风险。
- 请求元数据校验请求 ID 和协议版本。
- 错误只返回稳定错误码、翻译键和可重试标记，不在 Rust 契约中散落用户可见中文。
- 本阶段没有启动监听器、子进程、数据库访问、文件读写或业务数据操作。

## 自动验证

执行结果：

```text
npm run build
cargo fmt -- --check
cargo test
```

- `npm run build`：通过（TypeScript 类型检查与 Vite 构建通过）。
- `cargo fmt -- --check`：通过。
- `cargo test`：通过，26 个测试全部通过。
- 构建产生的 linker 信息为 Windows 调试库生成提示，不影响测试结果。

## 人工验收步骤

1. 查看 `mcp.rs` 的 `contract()`，确认工具清单覆盖读取、写入和高风险操作。
2. 确认 `mylist_import_confirm`、`mylist_delete_task`、`mylist_delete_category`、`mylist_export_confirm` 的 `requires_confirmation` 为 `true`。
3. 对照 Rust 与 TypeScript 的协议版本和错误码，确认没有传输层或数据访问实现。

## 已知限制

- 尚未实现 MCP 传输、服务开关、启动/停止状态或 IPC command。
- 尚未接入任务、分类、导入导出业务。

## 回滚点

- 删除本阶段新增的 `mcp.rs`、`src/mcp/contracts.ts` 和本记录，并移除 `lib.rs` 中的 `mod mcp;` 即可回滚。
