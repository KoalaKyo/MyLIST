# M03 任务核心验收记录

## 范围

- 已完成：SQLite 任务创建、查看、编辑、待办/已完成切换、完成、恢复、软删除、任务计数与重启后持久化。
- 已完成：任务必须关联有效分类；标题校验；任务 UUID、UTC 更新时间、版本字段与删除标记持续保留。
- 已完成：主窗口无原生标题栏后的拖动、右下角缩放热区、最小尺寸 350 × 530 DIP、内容区独立滚动及基础主题令牌还原。
- 未包含：任务行两段确认胶囊、完整右键/悬停菜单、截断 Tooltip 与动画验收（阶段 4）。
- 未包含：分类维护、截止时间与提醒、Windows 原生通知、导入导出（后续阶段）。

## 自动验证

- 命令：`npm run build`
- 结果：通过。
- 命令：`cargo test`
- 结果：通过（6 项 Rust 测试）。
- 命令：`npm run tauri build -- --debug`
- 结果：通过，生成调试 EXE。

## 人工验收

1. 操作：添加一项任务并保存。
   - 预期：返回主界面，任务出现在待办列表，重启后仍存在。
2. 操作：打开任务查看页、进入编辑并保存。
   - 预期：修改持久化，保存后返回主界面。
3. 操作：完成任务、切换至已完成、恢复任务。
   - 预期：数量与列表位置同步更新，恢复后回到待办。
4. 操作：删除任务并重启应用。
   - 预期：删除任务不再显示；数据层保留软删除标记供后续同步使用。
5. 操作：拖动各页面标题栏、在右下角缩放热区缩放窗口。
   - 预期：窗口可拖动、可缩放且不小于 350 × 530 DIP。
6. 操作：在浅色、深色桌面背景下检查无原生标题栏窗口。
   - 预期：窗口内容圆角稳定，主内容区左右使用标题栏同色的 1 DIP 内嵌视觉边界。

## 已知限制

- 截止时间、提醒和逾期排序将在阶段 6 接入；当前无时间字段的任务右侧保持留白。
- 当前视觉交互仅完成阶段 3 所需基础状态；阶段 4 将集中实现确认胶囊、菜单、Tooltip 和 300ms 动画。
- 系统原生外阴影在深色桌面下存在平台渲染不一致，当前采用无原生阴影方案。

## 回滚点

- Git 提交：`69efa09 feat(m03): complete task core and pass acceptance`。
- Git 标签：`v0.3-m03-approved`。

## 结论

- 状态：通过。
- 确认日期：2026-08-22。
