# M04 主界面精细交互验收记录

## 范围

- 已完成：待办与已完成事项的分类色圆环、悬停操作圆形、两段式完成/恢复/删除胶囊确认交互。
- 已完成：完成、恢复、删除胶囊的 300ms 前段快速缓动；标题同步压缩或回位；超长标题省略与窗口外 Tooltip。
- 已完成：待办完成向右、已完成恢复向左的任务离场动画：横向离场 600ms，第 300ms 起下方列表以 300ms 回流，二者同时结束。
- 已完成：任务右侧菜单、删除确认、独立浮层、外部点击关闭、时间隐藏规则及菜单三点规范。
- 已完成：主界面标题/页签/底栏固定、列表独立滚动、无箭头滚动条、加号位置、右下角点阵与缩放热区细节。
- 未包含：设置与分类维护、主题切换入口、截止时间与提醒、Windows 通知、导入导出（后续阶段）。

## 自动验证

- 命令：`npm run build`
  - 结果：通过。
- 命令：`cargo test`（`src-tauri` 目录）
  - 结果：通过，6 项 Rust 测试全部通过。
- 命令：`npm run tauri build -- --debug`
  - 结果：通过，调试 EXE 已生成并启动验证。

## 人工验收

1. 悬停待办与已完成事项，检查分类色圆环、20 DIP 默认操作、28 DIP 悬停操作和实心确认样式。
2. 分别完成与恢复事项，检查胶囊展开/收起、图标与文字同步、标题截断、Tooltip 和按钮互斥。
3. 完成待办与恢复已完成事项，检查 600ms 左右离场，以及 300ms 后启动、300ms 完成的下方列表回流。
4. 在已完成列表展开删除确认，检查标题向左压缩、删除图标回位、红色过渡与二次确认删除。
5. 检查三点菜单默认透明、悬停背景、窗口外浮层、点击其他位置关闭与独立显示。
6. 检查主界面滚动、底栏/加号固定、右下角点阵位置和窗口缩放热区。

## 已知限制

- 深色模式的主题令牌已在主界面组件中接入，但设置页和主题切换入口属于阶段 5，尚未进行完整人工视觉验收。
- 当前阶段不提供分类增删改、调色板选择和分类删除后的关联任务迁移。

## 回滚点

- Git 提交：`0613e45 feat(m04): complete polished task interactions and pass acceptance`。
- Git 标签：`v0.4-m04-approved`。

## 结论

- 状态：通过。
- 人工确认日期：2026-08-24。
